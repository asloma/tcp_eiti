CC=gcc
CFLAGS=-c -Wall

all: tcp_client

tcp_client: main.o listen_module.o  parser_module.o communication_module.o execute_module.o 
	$(CC)  client.o -o tcp_client

client.o: client.c client.h
	$(CC) $(CFLAGS) client.c client.h

clean:
	rm -rf *o tcp_server
